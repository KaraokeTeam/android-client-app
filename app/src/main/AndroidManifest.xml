<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.orpriesender.karaoke">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logosmall"
        android:logo="@drawable/logosmall"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logosmall"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.AuthActivity">


        </activity>

        <activity android:name=".controller.ResultActivity">

        </activity>

        <activity android:name=".controller.UserProfileActivity">

        </activity>>
        <activity android:name=".controller.FeedActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".controller.TarsosActivity">

        </activity>
    </application>

</manifest>